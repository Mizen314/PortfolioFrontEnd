# PortFolioEmma

This project was generated with [Angular CLI](https://github.com/angular/angular-cli) version 15.1.2. Este proyecto es el que representa el portfolio actualizacdo al 3/8/24. Dentro de feature/sextaEntrega. Vinculado a Netlify.
Recordar modificar archivos y correr el build.

## Build

Run `ng build` to build the project. The build artifacts will be stored in the `dist/` directory.
